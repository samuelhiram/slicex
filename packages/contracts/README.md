# @slicex/contracts

Shared DTOs, Zod schemas and the `ErrorEnvelope` used across packages and apps.

Keep DTOs minimal and stable; bump package carefully when changing shapes.

Usage:

```ts
import { TimelineDocumentSchema } from '@slicex/contracts';
```
