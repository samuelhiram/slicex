# @slicex/db

Prisma schema, migrations and database access helpers. All database access
should go through this package to keep a single source of truth for persistence.

Usage:

```ts
import prisma from "@slicex/db";
```
