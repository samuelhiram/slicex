# @slicex/testing

Helpers and fixtures for unit and integration tests across the monorepo.

Expose lightweight factories and common mocks used by package tests.

Usage:

```ts
import { makeTimelineFixture } from "@slicex/testing";
```
